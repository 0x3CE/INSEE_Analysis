# Évite les hoisting conflicts entre workspaces
node-linker=hoisted

# Cache local au projet (évite les collisions entre projets)
cache=.npm-cache

# Exact versions par défaut (pas de ^ ni ~)
save-exact=true
